server:
  port: 9100
spring:
  datasource:
    url: jdbc:mysql://localhost/Attendance?useSSL=false
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
admin:
  person:
    userName: admin
    password: admin
    authorities: ROLE_ADMIN, ROLE_EMPLOYEE
